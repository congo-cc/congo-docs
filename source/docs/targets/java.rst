Java
====

*Scope:* Java code generation, idioms, embedding, and runtime expectations (note: JDK_TARGET is a deprecated no-op).

.. todo::

   Draft this chapter. Java code generation, idioms, embedding, and runtime expectations (note: JDK_TARGET is a deprecated no-op).
