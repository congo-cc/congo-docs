Build and Runtime Matrix
========================

*Scope:* a side-by-side of per-language build steps and runtime-library expectations.

.. todo::

   Draft this chapter. a side-by-side of per-language build steps and runtime-library expectations.
